name: Deploy CITYPULSE Dashboard

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v6

      - name: Build dashboard
        run: |
          rm -rf _site
          mkdir _site
          cp dashboard/index.html _site/index.html
          cp dashboard/app.js _site/app.js
          cp dashboard/styles.css _site/styles.css
          cp dashboard/report.html _site/report.html

          # GitHub Pages is serving the dashboard files from the site root.
          # Convert root-absolute local paths to project-site-relative paths.
          sed -i 's|href="/styles.css"|href="styles.css"|g; s|src="/app.js"|src="app.js"|g; s|href="/report"|href="report.html"|g' _site/index.html
          sed -i 's|href="/styles.css"|href="styles.css"|g; s|src="/app.js"|src="app.js"|g; s|href="/report"|href="report.html"|g' _site/report.html

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
